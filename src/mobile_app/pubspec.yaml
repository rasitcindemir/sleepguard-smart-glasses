name: sleepguard
description: Mobile Application-Integrated Driver Sleep Warning System.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.65.0
  provider: ^6.1.1
  permission_handler: ^11.1.0
  flutter_bluetooth_classic_serial: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
